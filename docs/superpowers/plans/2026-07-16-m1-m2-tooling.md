# Piano di implementazione: tooling M1 (World Core) + M2 (Semantic QA)

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Rendere eseguibili validazione SHACL e query SPARQL sul mondo del roguelike semantico, portare il dataset al target di Fase 1 e dimostrare che ogni vincolo intercetta dati volutamente rotti (chiude M1 e M2 di `obiettivo.md`).

**Architecture:** Pipeline CLI in Python: `tools/` contiene moduli sottili (`common.py` per il caricamento grafi, `validate.py` per pySHACL, `query.py` per i file `.rq` in `queries/`); `tests/` contiene lo smoke test, i test delle query e i casi rotti in `tests/invalid/` verificati con pytest. I quattro moduli semantici (Model/Content/Validation + tooling) restano in cartelle separate.

**Tech Stack:** Python 3.12 (`C:\Users\D.DelPinto\AppData\Local\Programs\Python\Python312\python.exe`), rdflib ≥ 7.0, pySHACL ≥ 0.30, pytest ≥ 8.0. Ambiente in `.venv/` (già in `.gitignore`).

**Spec di riferimento:** `docs/superpowers/specs/2026-07-16-m1-m2-tooling-design.md`

## Global Constraints

- Namespace: `sr:` = `http://example.org/semantic-roguelike#` solo per vocabolario e shape; `ex:` = `http://example.org/id/` solo per istanze.
- Ogni risorsa TTL nuova o modificata ha `rdfs:label` e `rdfs:comment` sia `@it` sia `@en`; le shape hanno anche `sh:message` (e `sh:name`/`sh:description` per le property shape) bilingui.
- Exit code di `tools/validate.py`: 0 = conforme, 1 = violazioni SHACL, 2 = errore di parsing.
- Tutti i comandi si lanciano dalla root del repo; il Python del venv è `.venv/Scripts/python` (Windows).
- Ogni task termina con test verdi e un commit; i messaggi di commit finiscono con `Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>`.
- Ogni task produce una nota di studio in `docs/notes/` (in italiano, con esempi presi dal codice del repo, 40-80 righe).
- La validazione unisce sempre ontologia + dataset nel data graph (le shape usano `sh:class` e `sh:targetClass` su sottoclassi: senza le triple `rdfs:subClassOf` non funzionano).

---

### Task 1: Ambiente, caricamento grafi e smoke test

**Files:**
- Create: `requirements.txt`, `pytest.ini`, `tools/__init__.py`, `tools/common.py`, `tests/test_load.py`, `docs/notes/01-rdf-turtle.md`

**Interfaces:**
- Produces: `tools.common.ROOT: Path`, `ONTOLOGY/DATASET/SHAPES: Path`, `load_graph(*paths: Path) -> rdflib.Graph`, `load_world(data_path: Path = DATASET) -> rdflib.Graph` (grafo unito ontologia+dataset). Tutti i task successivi importano da qui.

- [ ] **Step 1: Crea ambiente e dipendenze**

`requirements.txt`:

```
rdflib>=7.0
pyshacl>=0.30
pytest>=8.0
```

Run:
```bash
python -m venv .venv
.venv/Scripts/python -m pip install -r requirements.txt
```
Expected: installazione senza errori (`pip list` mostra rdflib, pyshacl, pytest).

- [ ] **Step 2: Configura pytest**

`pytest.ini`:

```ini
[pytest]
pythonpath = .
testpaths = tests
```

`tools/__init__.py`: file vuoto.

- [ ] **Step 3: Scrivi lo smoke test (fallirà: common.py non esiste)**

`tests/test_load.py`:

```python
"""Smoke test: i tre file Turtle si caricano e si fondono in un grafo."""
from rdflib import Graph

from tools.common import DATASET, ONTOLOGY, SHAPES, load_graph, load_world


def test_each_ttl_file_parses():
    for path in (ONTOLOGY, DATASET, SHAPES):
        g = Graph()
        g.parse(path, format="turtle")
        assert len(g) > 0, f"{path} non contiene triple"


def test_load_world_merges_ontology_and_dataset():
    dataset_only = Graph()
    dataset_only.parse(DATASET, format="turtle")
    world = load_world()
    assert len(world) > len(dataset_only)


def test_load_graph_accepts_multiple_paths():
    g = load_graph(ONTOLOGY, DATASET)
    assert len(g) > 0
```

- [ ] **Step 4: Verifica che fallisca**

Run: `.venv/Scripts/python -m pytest -v`
Expected: FAIL/ERROR con `ModuleNotFoundError: No module named 'tools.common'`.

- [ ] **Step 5: Implementa `tools/common.py`**

```python
"""Caricamento dei grafi del progetto: Model (ontologia), Content (dataset), Validation (shape)."""
from pathlib import Path

from rdflib import Graph

ROOT = Path(__file__).resolve().parent.parent
ONTOLOGY = ROOT / "ontology" / "rogue.ttl"
DATASET = ROOT / "dataset" / "dataset.ttl"
SHAPES = ROOT / "shacl" / "rogue-rules.ttl"


def load_graph(*paths: Path) -> Graph:
    """Carica e fonde uno o più file Turtle in un unico grafo."""
    g = Graph()
    for path in paths:
        g.parse(path, format="turtle")
    return g


def load_world(data_path: Path = DATASET) -> Graph:
    """Grafo unito ontologia + dataset: la base per query e validazione."""
    return load_graph(ONTOLOGY, data_path)
```

- [ ] **Step 6: Verifica che passi**

Run: `.venv/Scripts/python -m pytest -v`
Expected: 3 PASS.

- [ ] **Step 7: Nota di studio** — `docs/notes/01-rdf-turtle.md`, in italiano, che copra con esempi presi da `dataset/dataset.ttl`: cos'è una tripla (soggetto-predicato-oggetto); prefissi e IRI (`sr:` vs `ex:` e perché sono separati); la scorciatoia `a` per `rdf:type`; letterali tipizzati (`sr:dangerLevel 4` → `xsd:integer`) e con language tag (`"Cripta 07"@it`); il `;` e il `.` nella sintassi Turtle; cosa fa `Graph.parse()` di rdflib e perché fondere ontologia+dataset dà più triple.

- [ ] **Step 8: Commit**

```bash
git add requirements.txt pytest.ini tools/ tests/test_load.py docs/notes/01-rdf-turtle.md
git commit -m "Task 1: ambiente Python, caricamento grafi e smoke test"
```

---

### Task 2: Validazione SHACL eseguibile + correzione del dataset

**Files:**
- Create: `tools/validate.py`, `tests/test_validation.py`, `docs/notes/02-shacl.md`
- Modify: `shacl/rogue-rules.ttl` (dichiarazione prefissi SPARQL), `dataset/dataset.ttl` (boss del santuario)

**Interfaces:**
- Consumes: `tools.common` (Task 1).
- Produces: `tests/test_validation.py::run_validation(data_path: Path) -> tuple[bool, rdflib.Graph, str]` (conforms, results_graph, results_text) riusata dal Task 5; CLI `python -m tools.validate [--data FILE]`.

**Contesto per l'esecutore:** le due shape SPARQL-based in `shacl/rogue-rules.ttl` usano il prefisso `sr:` dentro `sh:select` ma il file non dichiara `sh:prefixes`: pySHACL non può eseguirle così. Inoltre il dataset contiene una violazione reale: `ex:sanctum01` ha `sr:dangerLevel 6` senza `sr:hostsMonster` (shape `sr:DangerousRoomMustHaveContent`). Entrambe le cose vanno sistemate in questo task, guidate dai test.

- [ ] **Step 1: Scrivi il test di conformità (fallirà)**

`tests/test_validation.py`:

```python
"""La validazione SHACL passa sul dataset authored e fallisce sui dati rotti."""
from pathlib import Path

from pyshacl import validate

from tools.common import DATASET, ONTOLOGY, SHAPES, load_graph


def run_validation(data_path: Path):
    """Valida ontologia+dati contro le shape. Ritorna (conforms, results_graph, results_text)."""
    data = load_graph(ONTOLOGY, data_path)
    shapes = load_graph(SHAPES)
    return validate(data, shacl_graph=shapes, advanced=True)


def test_authored_dataset_conforms():
    conforms, _, text = run_validation(DATASET)
    assert conforms, f"Il dataset authored deve essere conforme alle shape:\n{text}"
```

- [ ] **Step 2: Verifica che fallisca**

Run: `.venv/Scripts/python -m pytest tests/test_validation.py -v`
Expected: FAIL — o un errore pySHACL sulle query SPARQL senza prefissi, o un report con la violazione di `sr:DangerousRoomMustHaveContent` su `ex:sanctum01`.

- [ ] **Step 3: Dichiara i prefissi per le shape SPARQL**

In `shacl/rogue-rules.ttl`: aggiungi `@prefix owl:  <http://www.w3.org/2002/07/owl#> .` in testa, poi subito dopo i prefissi:

```turtle
sr:RogueShapes a owl:Ontology ;
  rdfs:label "Shape SHACL del Roguelike Semantico"@it ;
  rdfs:label "Semantic Roguelike SHACL shapes"@en ;
  rdfs:comment "Documento delle shape con le dichiarazioni di prefisso usate dalle query SPARQL dei vincoli."@it ;
  rdfs:comment "Shapes document carrying the prefix declarations used by the SPARQL-based constraints."@en ;
  sh:declare [
    sh:prefix "sr" ;
    sh:namespace "http://example.org/semantic-roguelike#"^^xsd:anyURI ;
  ] .
```

e in **entrambi** i blocchi `sh:sparql [ ... ]` (di `sr:DangerousRoomMustHaveContent` e `sr:RequiredKeyMustExistInWorld`) aggiungi la riga `sh:prefixes sr:RogueShapes ;` prima di `sh:message`.

- [ ] **Step 4: Correggi il dataset (boss del santuario)**

In `dataset/dataset.ttl` aggiungi in fondo:

```turtle
ex:tyrantSigil a sr:Item ;
  rdfs:label "Sigillo del Tiranno"@it ;
  rdfs:label "Tyrant's Sigil"@en ;
  rdfs:comment "Emblema raro lasciato dal guardiano del santuario, prova della sua sconfitta."@it ;
  rdfs:comment "Rare emblem left by the sanctum guardian, proof of its defeat."@en ;
  sr:rarity 7 .

ex:cryptTyrant a sr:Boss ;
  rdfs:label "Tiranno della Cripta"@it ;
  rdfs:label "Crypt Tyrant"@en ;
  rdfs:comment "Boss non morto che presidia il santuario più profondo delle catacombe."@it ;
  rdfs:comment "Undead boss guarding the deepest sanctum of the catacombs."@en ;
  sr:belongsToFaction ex:deadCourt ;
  sr:dropsItem ex:tyrantSigil ;
  sr:spawnWeight 3 ;
  sr:minPlayerLevel 4 .
```

e alla risorsa esistente `ex:sanctum01` aggiungi la riga `sr:hostsMonster ex:cryptTyrant ;` (prima di `sr:locatedInBiome`).

- [ ] **Step 5: Verifica che passi**

Run: `.venv/Scripts/python -m pytest tests/test_validation.py -v`
Expected: PASS. (Se pySHACL segnala ancora errori di prefissi, controlla che `sh:prefixes sr:RogueShapes` sia dentro il nodo `sh:sparql`, non nella node shape.)

- [ ] **Step 6: Scrivi la CLI `tools/validate.py`**

```python
"""Valida il dataset contro le shape SHACL.

Uso: python -m tools.validate [--data FILE.ttl]
Exit code: 0 = conforme, 1 = violazioni, 2 = errore di parsing.
"""
import argparse
import sys
from pathlib import Path

from pyshacl import validate

from tools.common import DATASET, ONTOLOGY, SHAPES, load_graph


def main() -> int:
    parser = argparse.ArgumentParser(description="Valida il mondo contro le shape SHACL.")
    parser.add_argument("--data", type=Path, default=DATASET,
                        help="file Turtle del dataset da validare (default: dataset/dataset.ttl)")
    args = parser.parse_args()

    try:
        data = load_graph(ONTOLOGY, args.data)
        shapes = load_graph(SHAPES)
    except FileNotFoundError as e:
        print(f"File non trovato: {e}", file=sys.stderr)
        return 2
    except Exception as e:  # errori di sintassi Turtle (BadSyntax) e simili
        print(f"Errore di parsing: {e}", file=sys.stderr)
        return 2

    conforms, _, text = validate(data, shacl_graph=shapes, advanced=True)
    print(text)
    return 0 if conforms else 1


if __name__ == "__main__":
    raise SystemExit(main())
```

- [ ] **Step 7: Verifica la CLI a mano**

Run: `.venv/Scripts/python -m tools.validate; echo "exit: $?"`
Expected: report `Conforms: True`, `exit: 0`.

Run: `.venv/Scripts/python -m tools.validate --data tests/no-such-file.ttl; echo "exit: $?"`
Expected: messaggio di errore su stderr, `exit: 2`.

- [ ] **Step 8: Nota di studio** — `docs/notes/02-shacl.md`: cos'è una node shape e una property shape (esempio: `sr:RoomShape`); `sh:targetClass` e come le sottoclassi entrano nel target (perché serve l'ontologia nel data graph); vincoli core usati (`sh:minCount`, `sh:maxCount`, `sh:class`, `sh:datatype`, `sh:minInclusive`); shape SPARQL-based con `$this` e perché serve `sh:declare`/`sh:prefixes`; struttura del validation report (focusNode, resultPath, sourceShape); la violazione reale trovata su `ex:sanctum01` come caso di studio.

- [ ] **Step 9: Commit**

```bash
git add shacl/rogue-rules.ttl dataset/dataset.ttl tools/validate.py tests/test_validation.py docs/notes/02-shacl.md
git commit -m "Task 2: validazione pySHACL eseguibile, prefissi SPARQL e boss del santuario"
```

---

### Task 3: Query SPARQL di lettura (chiude la parte query di M1)

**Files:**
- Create: `queries/01-rooms-overview.rq`, `queries/02-connections.rq`, `queries/03-monsters-by-room.rq`, `queries/04-lock-key-chain.rq`, `queries/05-quests.rq`, `queries/06-items-by-rarity.rq`, `tools/query.py`, `tests/test_queries.py`, `docs/notes/03-sparql.md`

**Interfaces:**
- Consumes: `tools.common.load_world` (Task 1).
- Produces: CLI `python -m tools.query <file.rq> [--data FILE]` e `python -m tools.query --all`; i file `.rq` sono autonomi (dichiarano i propri `PREFIX`).

- [ ] **Step 1: Scrivi il test delle query (fallirà: la cartella non esiste)**

`tests/test_queries.py`:

```python
"""Ogni query in queries/ esegue senza errori e restituisce almeno una riga sul mondo authored."""
from pathlib import Path

import pytest

from tools.common import ROOT, load_world

QUERIES = sorted((ROOT / "queries").glob("*.rq"))


def test_query_directory_is_populated():
    assert len(QUERIES) >= 5, "attese almeno 5 query in queries/"


@pytest.mark.parametrize("rq_path", QUERIES, ids=lambda p: p.name)
def test_query_runs_and_returns_rows(rq_path):
    world = load_world()
    result = world.query(rq_path.read_text(encoding="utf-8"))
    assert len(list(result)) >= 1, f"{rq_path.name} non restituisce righe"
```

Run: `.venv/Scripts/python -m pytest tests/test_queries.py -v`
Expected: FAIL su `test_query_directory_is_populated` (0 query trovate).

- [ ] **Step 2: Scrivi le sei query**

`queries/01-rooms-overview.rq`:

```sparql
# Panoramica delle stanze: bioma, livello di pericolo e piano.
# Risponde a: "com'è fatta la mappa, piano per piano?"
PREFIX sr:   <http://example.org/semantic-roguelike#>
PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>

SELECT ?stanza ?bioma ?pericolo ?piano
WHERE {
  ?room a sr:Room ;
        rdfs:label ?stanza ;
        sr:locatedInBiome/rdfs:label ?bioma ;
        sr:dangerLevel ?pericolo ;
        sr:floorIndex ?piano .
  FILTER(lang(?stanza) = "it" && lang(?bioma) = "it")
}
ORDER BY ?piano ?pericolo
```

`queries/02-connections.rq`:

```sparql
# Mappa dei collegamenti diretti fra stanze.
# Risponde a: "da quale stanza si va dove?"
PREFIX sr:   <http://example.org/semantic-roguelike#>
PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>

SELECT ?da ?a
WHERE {
  ?r1 sr:connectedTo ?r2 .
  ?r1 rdfs:label ?da .
  ?r2 rdfs:label ?a .
  FILTER(lang(?da) = "it" && lang(?a) = "it")
}
ORDER BY ?da
```

`queries/03-monsters-by-room.rq`:

```sparql
# Mostri per stanza, con fazione e livello minimo del giocatore.
# Risponde a: "chi incontro e dove, e sono pronto per farlo?"
PREFIX sr:   <http://example.org/semantic-roguelike#>
PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>

SELECT ?stanza ?mostro ?fazione ?livelloMinimo
WHERE {
  ?room sr:hostsMonster ?m ;
        rdfs:label ?stanza .
  ?m rdfs:label ?mostro ;
     sr:belongsToFaction/rdfs:label ?fazione ;
     sr:minPlayerLevel ?livelloMinimo .
  FILTER(lang(?stanza) = "it" && lang(?mostro) = "it" && lang(?fazione) = "it")
}
ORDER BY ?stanza ?mostro
```

`queries/04-lock-key-chain.rq`:

```sparql
# Catena lock-key: portale -> chiave richiesta -> chi la droppa -> in quale stanza.
# Risponde a: "dove ottengo la chiave che apre questo portale?"
PREFIX sr:   <http://example.org/semantic-roguelike#>
PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>

SELECT ?portale ?chiave ?droppataDa ?stanzaDrop ?stanzaSbloccata
WHERE {
  ?p a sr:Portal ;
     rdfs:label ?portale ;
     sr:requiresItem ?k ;
     sr:unlocksRoom ?locked .
  ?k rdfs:label ?chiave .
  ?locked rdfs:label ?stanzaSbloccata .
  OPTIONAL {
    ?m sr:dropsItem ?k ;
       rdfs:label ?droppataDa .
    FILTER(lang(?droppataDa) = "it")
    OPTIONAL {
      ?room sr:hostsMonster ?m ;
            rdfs:label ?stanzaDrop .
      FILTER(lang(?stanzaDrop) = "it")
    }
  }
  FILTER(lang(?portale) = "it" && lang(?chiave) = "it" && lang(?stanzaSbloccata) = "it")
}
```

`queries/05-quests.rq`:

```sparql
# Quadro delle quest: chi le offre, dove portano, cosa premiano.
# Risponde a: "quali missioni esistono e valgono la pena?"
PREFIX sr:   <http://example.org/semantic-roguelike#>
PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>

SELECT ?quest ?offertaDa ?stanzaObiettivo ?ricompensa ?rarita
WHERE {
  ?q a sr:Quest ;
     rdfs:label ?quest ;
     sr:questTargetRoom/rdfs:label ?stanzaObiettivo ;
     sr:questReward ?r .
  ?r rdfs:label ?ricompensa ;
     sr:rarity ?rarita .
  OPTIONAL {
    ?npc sr:offersQuest ?q ;
         rdfs:label ?offertaDa .
    FILTER(lang(?offertaDa) = "it")
  }
  FILTER(lang(?quest) = "it" && lang(?stanzaObiettivo) = "it" && lang(?ricompensa) = "it")
}
ORDER BY DESC(?rarita)
```

`queries/06-items-by-rarity.rq`:

```sparql
# Oggetti ordinati per rarita', con il tipo piu' specifico.
# Il path rdfs:subClassOf* mostra perche' l'ontologia deve stare nel grafo.
PREFIX sr:   <http://example.org/semantic-roguelike#>
PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>

SELECT ?oggetto ?tipo ?rarita
WHERE {
  ?i a ?cls ;
     rdfs:label ?oggetto ;
     sr:rarity ?rarita .
  ?cls rdfs:subClassOf* sr:Item ;
       rdfs:label ?tipo .
  FILTER(lang(?oggetto) = "it" && lang(?tipo) = "it")
}
ORDER BY DESC(?rarita) ?oggetto
```

- [ ] **Step 3: Scrivi la CLI `tools/query.py`**

```python
"""Esegue query SPARQL (.rq) sul grafo unito ontologia+dataset.

Uso: python -m tools.query queries/01-rooms-overview.rq [--data FILE.ttl]
     python -m tools.query --all
"""
import argparse
import sys
from pathlib import Path

from tools.common import DATASET, ROOT, load_world


def format_table(result) -> str:
    """Formatta un risultato SELECT di rdflib come tabella testuale allineata."""
    headers = [str(v) for v in result.vars]
    rows = [["" if cell is None else str(cell) for cell in row] for row in result]
    widths = [max(len(h), *(len(r[i]) for r in rows)) if rows else len(h)
              for i, h in enumerate(headers)]
    def line(cells):
        return "  ".join(c.ljust(w) for c, w in zip(cells, widths))
    out = [line(headers), line(["-" * w for w in widths])]
    out += [line(r) for r in rows]
    out.append(f"({len(rows)} righe)")
    return "\n".join(out)


def run_file(world, rq_path: Path) -> int:
    try:
        text = rq_path.read_text(encoding="utf-8")
    except FileNotFoundError:
        print(f"Query non trovata: {rq_path}", file=sys.stderr)
        return 2
    try:
        result = world.query(text)
    except Exception as e:
        print(f"Query malformata in {rq_path.name}: {e}", file=sys.stderr)
        return 2
    print(f"== {rq_path.name} ==")
    print(format_table(result))
    print()
    return 0


def main() -> int:
    parser = argparse.ArgumentParser(description="Esegue query SPARQL sul mondo.")
    parser.add_argument("query", nargs="?", type=Path, help="file .rq da eseguire")
    parser.add_argument("--all", action="store_true", help="esegue tutte le query in queries/")
    parser.add_argument("--data", type=Path, default=DATASET, help="dataset alternativo")
    args = parser.parse_args()

    if not args.all and args.query is None:
        parser.error("indicare un file .rq oppure --all")

    world = load_world(args.data)
    targets = sorted((ROOT / "queries").glob("*.rq")) if args.all else [args.query]
    return max((run_file(world, t) for t in targets), default=0)


if __name__ == "__main__":
    raise SystemExit(main())
```

- [ ] **Step 4: Verifica che i test passino**

Run: `.venv/Scripts/python -m pytest -v`
Expected: tutti PASS (smoke test, validazione, 1 test di popolamento + 6 test di query).

- [ ] **Step 5: Prova la CLI e leggi davvero i risultati**

Run: `.venv/Scripts/python -m tools.query --all`
Expected: sei tabelle; controlla a mano che `04-lock-key-chain.rq` mostri la Porta Sigillata con Chiave d'Avorio droppata dal Custode d'Ossa in Cripta 07.

- [ ] **Step 6: Nota di studio** — `docs/notes/03-sparql.md`: struttura di una SELECT (basic graph pattern, variabili); property path (`/` in `sr:locatedInBiome/rdfs:label`, `*` in `rdfs:subClassOf*`); `OPTIONAL` e i suoi annidamenti (esempio dalla query 04: perché la stanza del drop è opzionale dentro l'opzionale del drop); `FILTER` e `lang()`; `ORDER BY`/`DESC`. Ogni concetto con riferimento al file `.rq` che lo usa.

- [ ] **Step 7: Commit**

```bash
git add queries/ tools/query.py tests/test_queries.py docs/notes/03-sparql.md
git commit -m "Task 3: query SPARQL di lettura con CLI e test (M1 query complete)"
```

---

### Task 4: Espansione del mondo al target di Fase 1 (con esercizio per l'autore)

**Files:**
- Modify: `ontology/rogue.ttl` (due proprietà nuove), `shacl/rogue-rules.ttl` (PortalShape e RoomShape aggiornate), `dataset/dataset.ttl` (mondo espanso), `queries/02-connections.rq` (include i portali)
- Create: `docs/notes/04-modellazione.md`

**Interfaces:**
- Consumes: `run_validation` (Task 2), CLI query (Task 3).
- Produces: proprietà `sr:portalInRoom` (Portal→Room) e `sr:isStartRoom` (Room→xsd:boolean), usate dalle shape del Task 5. Dataset con 10 stanze, 3 fazioni, 8 mostri, 10 item, 3 quest, 2 NPC, 2 portali, 2 biomi.

**Contesto per l'esecutore:** il modello attuale ha due lacune che l'espansione rende evidenti: un portale non dice in quale stanza si trova, e nessuna stanza è marcata come iniziale (senza queste due cose la raggiungibilità del Task 5 non è definibile). Si estende l'ontologia PRIMA di aggiungere contenuto.

- [ ] **Step 1: Estendi l'ontologia**

In `ontology/rogue.ttl`, sezione object properties:

```turtle
sr:portalInRoom a owl:ObjectProperty ;
  rdfs:domain sr:Portal ;
  rdfs:range sr:Room ;
  rdfs:label "si trova nella stanza"@it ;
  rdfs:label "located in room"@en ;
  rdfs:comment "Indica la stanza in cui il portale è fisicamente collocato e da cui lo si attraversa."@it ;
  rdfs:comment "Indicates the room where the portal is physically located and from which it is traversed."@en .
```

Sezione datatype properties:

```turtle
sr:isStartRoom a owl:DatatypeProperty ;
  rdfs:domain sr:Room ;
  rdfs:range xsd:boolean ;
  rdfs:label "è la stanza iniziale"@it ;
  rdfs:label "is start room"@en ;
  rdfs:comment "Vero per la stanza in cui il giocatore inizia la run; la raggiungibilità del mondo si calcola da qui."@it ;
  rdfs:comment "True for the room where the player starts the run; world reachability is computed from here."@en .
```

- [ ] **Step 2: Aggiorna le shape coerentemente (regola del progetto: ogni aggiunta ontologica valuta la sua shape)**

In `shacl/rogue-rules.ttl`, dentro `sr:PortalShape` aggiungi una terza property shape:

```turtle
  sh:property [
    sh:path sr:portalInRoom ;
    sh:minCount 1 ;
    sh:maxCount 1 ;
    sh:class sr:Room ;
    sh:name "stanza del portale"@it ;
    sh:name "portal room"@en ;
    sh:description "Ogni portale deve trovarsi in esattamente una stanza, altrimenti non è attraversabile."@it ;
    sh:description "Each portal must be located in exactly one room, otherwise it cannot be traversed."@en ;
  ] ;
```

e dentro `sr:RoomShape`:

```turtle
  sh:property [
    sh:path sr:isStartRoom ;
    sh:maxCount 1 ;
    sh:datatype xsd:boolean ;
    sh:name "flag stanza iniziale"@it ;
    sh:name "start room flag"@en ;
    sh:description "Il flag di stanza iniziale, se presente, è un booleano singolo."@it ;
    sh:description "The start-room flag, when present, is a single boolean."@en ;
  ] ;
```

Run: `.venv/Scripts/python -m pytest -v`
Expected: FAIL su `test_authored_dataset_conforms` — `ex:sealedGate` ora viola `sr:portalInRoom minCount 1`. Questo è il test che guida lo step successivo.

- [ ] **Step 3: Espandi il dataset (parte dell'esecutore)**

Piano del mondo (i valori sono la specifica; label bilingui e comment bilingui obbligatori per ogni entità — seguire lo stile delle entità esistenti nel file):

**Biomi** — nuovo: `ex:fungalCaves` "Grotte Fungine"/"Fungal Caves".

**Fazioni** — nuova: `ex:sporeMind` "Mente Sporale"/"Spore Mind", `sr:enemyOf ex:playerFaction`.

**Stanze** (esistenti: crypt07, hall08, sanctum01):

| id | label it / en | bioma | danger | floor | connectedTo | altro |
|---|---|---|---|---|---|---|
| `ex:entrance01` | Ingresso delle Catacombe / Catacombs Entrance | catacomb | 1 | 1 | ossuary02 | `sr:isStartRoom true` |
| `ex:ossuary02` | Ossario 02 / Ossuary 02 | catacomb | 2 | 1 | crypt07 | hostsMonster cryptRat |
| `ex:fungalHollow01` | Cavità Fungina / Fungal Hollow | fungalCaves | 4 | 2 | sporeNest02 | hostsMonster sporeCrawler |
| `ex:sporeNest02` | Nido di Spore / Spore Nest | fungalCaves | 5 | 3 | — | hostsMonster myceliumBrute, sporeMother |
| `ex:throneVault01` | Cripta del Trono / Throne Vault | catacomb | 8 | 4 | — | hostsMonster deadKing; raggiunta solo via portale |

Modifiche a stanze esistenti: `ex:hall08` aggiunge `sr:connectedTo ex:fungalHollow01`.

**Mostri** (esistenti: boneWarden, cryptTyrant):

| id | label it / en | classe | fazione | spawn | lvlMin | droppa | ospitato in |
|---|---|---|---|---|---|---|---|
| `ex:cryptRat` | Ratto delle Cripte / Crypt Rat | Monster | deadCourt | 20 | 1 | rustSword | ossuary02 |
| `ex:sporeCrawler` | Strisciante di Spore / Spore Crawler | Monster | sporeMind | 15 | 2 | healingMoss | fungalHollow01 |
| `ex:myceliumBrute` | Bruto di Micelio / Mycelium Brute | Monster | sporeMind | 6 | 3 | mossAmulet | sporeNest02 |
| `ex:sporeMother` | Madre delle Spore / Spore Mother | **Boss** | sporeMind | 2 | 4 | sporeHeart | sporeNest02 |
| `ex:deadKing` | Re Morto / Dead King | **Boss** | deadCourt | 1 | 5 | relicOfDawn | throneVault01 |

**Item** (esistenti: ivoryKey, tyrantSigil):

| id | label it / en | classe | rarity |
|---|---|---|---|
| `ex:rustSword` | Spada Arrugginita / Rusty Sword | Weapon | 2 |
| `ex:boneBlade` | Lama d'Osso / Bone Blade | Weapon | 5 |
| `ex:healingMoss` | Muschio Curativo / Healing Moss | Consumable | 1 |
| `ex:embalmerTonic` | Tonico dell'Imbalsamatore / Embalmer's Tonic | Consumable | 3 |
| `ex:graveLantern` | Lanterna Tombale / Grave Lantern | Item | 3 |
| `ex:mossAmulet` | Amuleto di Muschio / Moss Amulet | Item | 4 |
| `ex:sporeHeart` | Cuore di Spore / Spore Heart | **KeyItem** | 6 |
| `ex:relicOfDawn` | Reliquia dell'Alba / Relic of Dawn | Item | 9 |

Aggiungi anche: `ex:cryptTyrant sr:dropsItem ex:graveLantern`.

**Portali** (esistente: sealedGate): `ex:sealedGate` aggiunge `sr:portalInRoom ex:crypt07`. Nuovo: `ex:mycelialSeal` "Sigillo Miceliale"/"Mycelial Seal", `sr:portalInRoom ex:sporeNest02`, `sr:requiresItem ex:sporeHeart`, `sr:unlocksRoom ex:throneVault01`.

**Quest e NPC**: nuova quest `ex:endTheCourt` "Poni Fine alla Corte"/"End the Court", `sr:questTargetRoom ex:throneVault01`, `sr:questReward ex:relicOfDawn`, offerta da `ex:graveHermit` (aggiungere `sr:offersQuest ex:endTheCourt` all'eremita).

L'esecutore scrive in Turtle **tutte le entità sopra tranne quelle riservate all'autore (Step 4)**.

- [ ] **Step 4: CHECKPOINT UTENTE — contenuto scritto a mano dall'autore come esercizio**

Restano da scrivere, a mano, da parte dell'autore del progetto (fermarsi e chiederglielo, poi revisionare il suo Turtle):

| entità | valori richiesti |
|---|---|
| `ex:chapel03` (Room) | Cappella Profanata / Desecrated Chapel, catacomb, danger 3, floor 2, connectedTo hall08; hostsMonster hollowMonk; collegata da entrance01 (`ex:entrance01 sr:connectedTo ex:chapel03`) |
| `ex:boneBridge04` (Room) | Ponte d'Ossa / Bone Bridge, catacomb, danger 5, floor 3; `ex:sanctum01 sr:connectedTo ex:boneBridge04` |
| `ex:hollowMonk` (Monster) | Monaco Cavo / Hollow Monk, deadCourt, spawn 8, lvlMin 2, droppa embalmerTonic, ospitato in chapel03 |
| `ex:ashPriest` (NPC) | Sacerdote di Cenere / Ash Priest, offre cullTheSpores |
| `ex:cullTheSpores` (Quest) | Estirpa le Spore / Cull the Spores, target sporeNest02, reward boneBlade |

Criterio di revisione: label/comment bilingui, namespace giusti, la validazione passa.

- [ ] **Step 5: Aggiorna la query delle connessioni per includere i portali**

Sostituisci il contenuto di `queries/02-connections.rq` con:

```sparql
# Mappa dei collegamenti fra stanze: passaggi aperti e portali con condizione.
# Risponde a: "da quale stanza si va dove, e cosa serve per passare?"
PREFIX sr:   <http://example.org/semantic-roguelike#>
PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>

SELECT ?da ?a ?tipo ?serve
WHERE {
  {
    ?r1 sr:connectedTo ?r2 .
    BIND("passaggio aperto" AS ?tipo)
  }
  UNION
  {
    ?p a sr:Portal ;
       sr:portalInRoom ?r1 ;
       sr:unlocksRoom ?r2 ;
       sr:requiresItem ?item .
    ?item rdfs:label ?serve .
    FILTER(lang(?serve) = "it")
    BIND("portale" AS ?tipo)
  }
  ?r1 rdfs:label ?da .
  ?r2 rdfs:label ?a .
  FILTER(lang(?da) = "it" && lang(?a) = "it")
}
ORDER BY ?da
```

- [ ] **Step 6: Verifica tutto**

Run: `.venv/Scripts/python -m pytest -v`
Expected: tutti PASS (la validazione ora copre il mondo espanso).

Run: `.venv/Scripts/python -m tools.query --all`
Expected: `01` mostra 10 stanze su 4 piani; `02` mostra i due portali con l'oggetto richiesto; `05` mostra 3 quest.

- [ ] **Step 7: Nota di studio** — `docs/notes/04-modellazione.md`: perché l'espansione ha richiesto due proprietà nuove (il gap "il portale non ha un luogo" e "nessuna stanza è l'inizio"); domain/range come documentazione vs vincolo (OWL non blocca nulla senza reasoner: sono le shape a farlo); scelte di naming (`ex:` con camelCase, label bilingui); il pattern "estendi ontologia → aggiorna shape → poi aggiungi dati" visto in pratica.

- [ ] **Step 8: Commit**

```bash
git add ontology/rogue.ttl shacl/rogue-rules.ttl dataset/dataset.ttl queries/02-connections.rq docs/notes/04-modellazione.md
git commit -m "Task 4: mondo espanso al target di Fase 1, portali collocati e stanza iniziale"
```

---

### Task 5: Shape di progressione + casi volutamente rotti (chiude M2)

**Files:**
- Modify: `shacl/rogue-rules.ttl` (3 shape nuove + 1 aggiornata), `tests/test_validation.py` (test parametrizzati)
- Create: `tests/invalid/*.ttl` (10 file), `docs/notes/05-shacl-avanzato.md`

**Interfaces:**
- Consumes: `run_validation` (Task 2), `sr:portalInRoom`/`sr:isStartRoom` (Task 4).
- Produces: shape `sr:OrphanKeyShape`, `sr:UnreachableRoomShape`, `sr:LockKeyCycleShape`; suite `tests/invalid/` completa.

**Contesto per l'esecutore:** ogni file in `tests/invalid/` è un micro-mondo autonomo (si valida da solo, unito alla sola ontologia — MAI al dataset buono) costruito per violare esattamente una shape: tutte le altre proprietà richieste sono presenti e valide. Il test verifica che la shape attesa compaia nel report per il focus node atteso: per le violazioni di property shape si controlla `sh:resultPath` (i property shape sono bnode anonimi, il loro `sh:sourceShape` non è confrontabile), per le shape SPARQL si controlla `sh:sourceShape`.

- [ ] **Step 1: Scrivi i test parametrizzati (falliranno: file e shape non esistono)**

Aggiungi in coda a `tests/test_validation.py`:

```python
import pytest
from rdflib import Namespace

SR = Namespace("http://example.org/semantic-roguelike#")
EX = Namespace("http://example.org/id/")
SH = Namespace("http://www.w3.org/ns/shacl#")
INVALID = Path(__file__).resolve().parent / "invalid"

# (file, focus node atteso, ("path", resultPath) per property shape
#  oppure ("shape", NodeShape) per vincoli SPARQL-based)
BROKEN_CASES = [
    ("room-no-biome.ttl", EX.brokenRoom, ("path", SR.locatedInBiome)),
    ("room-danger-out-of-range.ttl", EX.brokenRoom, ("path", SR.dangerLevel)),
    ("monster-no-faction.ttl", EX.brokenMonster, ("path", SR.belongsToFaction)),
    ("boss-no-loot.ttl", EX.brokenBoss, ("path", SR.dropsItem)),
    ("portal-nowhere.ttl", EX.brokenPortal, ("path", SR.portalInRoom)),
    ("dangerous-room-empty.ttl", EX.brokenRoom, ("shape", SR.DangerousRoomMustHaveContent)),
    ("portal-unobtainable-key.ttl", EX.brokenPortal, ("shape", SR.RequiredKeyMustExistInWorld)),
    ("orphan-key.ttl", EX.orphanKey, ("shape", SR.OrphanKeyShape)),
    ("unreachable-room.ttl", EX.lostRoom, ("shape", SR.UnreachableRoomShape)),
    ("lock-key-cycle.ttl", EX.cyclePortal, ("shape", SR.LockKeyCycleShape)),
]


@pytest.mark.parametrize("fname,focus,expected", BROKEN_CASES, ids=[c[0] for c in BROKEN_CASES])
def test_broken_case_triggers_expected_shape(fname, focus, expected):
    conforms, results, text = run_validation(INVALID / fname)
    assert not conforms, f"{fname} dovrebbe violare le shape"
    kind, value = expected
    prop = SH.resultPath if kind == "path" else SH.sourceShape
    matching = [r for r in results.subjects(SH.focusNode, focus)
                if (r, prop, value) in results]
    assert matching, f"attesa violazione {value} su {focus} in {fname}; report:\n{text}"
```

Run: `.venv/Scripts/python -m pytest tests/test_validation.py -v`
Expected: 10 FAIL (file mancanti), 1 PASS (conformità del dataset).

- [ ] **Step 2: Scrivi le shape nuove in `shacl/rogue-rules.ttl`**

```turtle
sr:OrphanKeyShape a sh:NodeShape ;
  sh:targetClass sr:KeyItem ;
  rdfs:label "Nessuna chiave orfana"@it ;
  rdfs:label "No orphan keys"@en ;
  rdfs:comment "Shape SPARQL-based: ogni oggetto chiave deve avere almeno una fonte nel mondo, come drop di un mostro o ricompensa di una quest."@it ;
  rdfs:comment "SPARQL-based shape: every key item must have at least one source in the world, either as a monster drop or as a quest reward."@en ;
  sh:sparql [
    sh:prefixes sr:RogueShapes ;
    sh:message "Ogni oggetto chiave deve essere ottenibile: droppato da un mostro o ricompensa di una quest."@it ;
    sh:message "Every key item must be obtainable: dropped by a monster or rewarded by a quest."@en ;
    sh:select """
      SELECT $this
      WHERE {
        FILTER NOT EXISTS { ?monster sr:dropsItem $this . }
        FILTER NOT EXISTS { ?quest sr:questReward $this . }
      }
    """ ;
  ] .

sr:UnreachableRoomShape a sh:NodeShape ;
  sh:targetClass sr:Room ;
  rdfs:label "Nessuna stanza irraggiungibile"@it ;
  rdfs:label "No unreachable rooms"@en ;
  rdfs:comment "Shape SPARQL-based: ogni stanza deve essere raggiungibile dalla stanza iniziale attraversando connessioni (in entrambi i versi) o portali."@it ;
  rdfs:comment "SPARQL-based shape: every room must be reachable from the start room by traversing connections (in both directions) or portals."@en ;
  sh:sparql [
    sh:prefixes sr:RogueShapes ;
    sh:message "La stanza non è raggiungibile dalla stanza iniziale: collegala o rimuovila."@it ;
    sh:message "The room is not reachable from the start room: connect it or remove it."@en ;
    sh:select """
      SELECT $this
      WHERE {
        FILTER NOT EXISTS {
          ?start sr:isStartRoom true .
          ?start (sr:connectedTo|^sr:connectedTo|(^sr:portalInRoom/sr:unlocksRoom))* $this .
        }
      }
    """ ;
  ] .

sr:LockKeyCycleShape a sh:NodeShape ;
  sh:targetClass sr:Portal ;
  rdfs:label "Nessuna dipendenza circolare lock-key"@it ;
  rdfs:label "No circular lock-key dependency"@en ;
  rdfs:comment "Shape SPARQL-based: la chiave di un portale deve avere almeno una fonte collocata fuori dalla stanza che il portale sblocca, altrimenti la chiave è irraggiungibile senza sé stessa."@it ;
  rdfs:comment "SPARQL-based shape: a portal's key must have at least one source located outside the room the portal unlocks, otherwise the key is unreachable without itself."@en ;
  sh:sparql [
    sh:prefixes sr:RogueShapes ;
    sh:message "La chiave di questo portale si ottiene solo oltre il portale stesso: dipendenza circolare."@it ;
    sh:message "This portal's key can only be obtained beyond the portal itself: circular dependency."@en ;
    sh:select """
      SELECT $this
      WHERE {
        $this sr:requiresItem ?key ;
              sr:unlocksRoom ?locked .
        FILTER NOT EXISTS {
          {
            ?monster sr:dropsItem ?key .
            ?source sr:hostsMonster ?monster .
            FILTER(?source != ?locked)
          }
          UNION
          {
            ?quest sr:questReward ?key ;
                   sr:questTargetRoom ?target .
            FILTER(?target != ?locked)
          }
        }
      }
    """ ;
  ] .
```

Aggiorna inoltre `sr:RequiredKeyMustExistInWorld`: nella sua `sh:select`, sostituisci

```
        FILTER NOT EXISTS {
          ?monster sr:dropsItem ?k .
        }
```

con

```
        FILTER NOT EXISTS {
          { ?monster sr:dropsItem ?k . }
          UNION
          { ?quest sr:questReward ?k . }
        }
```

(una chiave che è solo ricompensa di quest è ottenibile: la shape non deve scattare).

- [ ] **Step 3: Scrivi i dieci micro-mondi rotti in `tests/invalid/`**

Tutti i file iniziano con:

```turtle
@prefix sr:   <http://example.org/semantic-roguelike#> .
@prefix ex:   <http://example.org/id/> .
@prefix rdfs: <http://www.w3.org/2000/01/rdf-schema#> .
```

Le label/comment bilingui NON sono richieste qui (sono fixture di test, non contenuto). Ogni file contiene il minimo perché scatti solo la shape bersaglio. Nei file che contengono stanze, marca una stanza con `sr:isStartRoom true` e collega tutto, così `sr:UnreachableRoomShape` non scatta per sbaglio.

`room-no-biome.ttl` (manca il bioma, il resto è valido):

```turtle
ex:brokenRoom a sr:Room ;
  sr:isStartRoom true ;
  sr:dangerLevel 2 ;
  sr:floorIndex 1 .
```

`room-danger-out-of-range.ttl`:

```turtle
ex:testBiome a sr:Biome .
ex:brokenRoom a sr:Room ;
  sr:isStartRoom true ;
  sr:locatedInBiome ex:testBiome ;
  sr:dangerLevel 15 ;
  sr:floorIndex 1 ;
  sr:hostsMonster ex:filler .
ex:testFaction a sr:Faction .
ex:filler a sr:Monster ;
  sr:belongsToFaction ex:testFaction ;
  sr:spawnWeight 1 ;
  sr:minPlayerLevel 1 .
```

`monster-no-faction.ttl`:

```turtle
ex:brokenMonster a sr:Monster ;
  sr:spawnWeight 5 ;
  sr:minPlayerLevel 1 .
```

`boss-no-loot.ttl`:

```turtle
ex:testFaction a sr:Faction .
ex:brokenBoss a sr:Boss ;
  sr:belongsToFaction ex:testFaction ;
  sr:spawnWeight 1 ;
  sr:minPlayerLevel 3 .
```

`portal-nowhere.ttl` (portale senza stanza che lo ospita):

```turtle
ex:testBiome a sr:Biome .
ex:roomA a sr:Room ;
  sr:isStartRoom true ;
  sr:locatedInBiome ex:testBiome ;
  sr:dangerLevel 1 ;
  sr:floorIndex 1 .
ex:roomB a sr:Room ;
  sr:locatedInBiome ex:testBiome ;
  sr:dangerLevel 1 ;
  sr:floorIndex 1 ;
  sr:connectedTo ex:roomA .
ex:roomA sr:connectedTo ex:roomB .
ex:testKey a sr:Item ;
  sr:rarity 3 .
ex:testFaction a sr:Faction .
ex:testMonster a sr:Monster ;
  sr:belongsToFaction ex:testFaction ;
  sr:spawnWeight 1 ;
  sr:minPlayerLevel 1 ;
  sr:dropsItem ex:testKey .
ex:roomA sr:hostsMonster ex:testMonster .
ex:brokenPortal a sr:Portal ;
  sr:requiresItem ex:testKey ;
  sr:unlocksRoom ex:roomB .
```

`dangerous-room-empty.ttl` (danger alto, nessun mostro):

```turtle
ex:testBiome a sr:Biome .
ex:brokenRoom a sr:Room ;
  sr:isStartRoom true ;
  sr:locatedInBiome ex:testBiome ;
  sr:dangerLevel 7 ;
  sr:floorIndex 1 .
```

`portal-unobtainable-key.ttl` (la chiave non è né drop né reward):

```turtle
ex:testBiome a sr:Biome .
ex:roomA a sr:Room ;
  sr:isStartRoom true ;
  sr:locatedInBiome ex:testBiome ;
  sr:dangerLevel 1 ;
  sr:floorIndex 1 .
ex:roomB a sr:Room ;
  sr:locatedInBiome ex:testBiome ;
  sr:dangerLevel 1 ;
  sr:floorIndex 1 .
ex:ghostKey a sr:Item ;
  sr:rarity 5 .
ex:brokenPortal a sr:Portal ;
  sr:portalInRoom ex:roomA ;
  sr:requiresItem ex:ghostKey ;
  sr:unlocksRoom ex:roomB .
```

`orphan-key.ttl` (KeyItem senza alcuna fonte; nessun portale la richiede, quindi scatta solo la shape delle chiavi orfane):

```turtle
ex:orphanKey a sr:KeyItem ;
  sr:rarity 4 .
```

`unreachable-room.ttl` (stanza scollegata da tutto):

```turtle
ex:testBiome a sr:Biome .
ex:startRoom a sr:Room ;
  sr:isStartRoom true ;
  sr:locatedInBiome ex:testBiome ;
  sr:dangerLevel 1 ;
  sr:floorIndex 1 .
ex:lostRoom a sr:Room ;
  sr:locatedInBiome ex:testBiome ;
  sr:dangerLevel 2 ;
  sr:floorIndex 2 .
```

`lock-key-cycle.ttl` (l'unica fonte della chiave è nella stanza sbloccata dal portale stesso):

```turtle
ex:testBiome a sr:Biome .
ex:startRoom a sr:Room ;
  sr:isStartRoom true ;
  sr:locatedInBiome ex:testBiome ;
  sr:dangerLevel 1 ;
  sr:floorIndex 1 .
ex:lockedRoom a sr:Room ;
  sr:locatedInBiome ex:testBiome ;
  sr:dangerLevel 6 ;
  sr:floorIndex 2 ;
  sr:hostsMonster ex:keyHolder .
ex:cycleKey a sr:KeyItem ;
  sr:rarity 5 .
ex:testFaction a sr:Faction .
ex:keyHolder a sr:Monster ;
  sr:belongsToFaction ex:testFaction ;
  sr:spawnWeight 1 ;
  sr:minPlayerLevel 1 ;
  sr:dropsItem ex:cycleKey .
ex:cyclePortal a sr:Portal ;
  sr:portalInRoom ex:startRoom ;
  sr:requiresItem ex:cycleKey ;
  sr:unlocksRoom ex:lockedRoom .
```

- [ ] **Step 4: Verifica che tutto passi**

Run: `.venv/Scripts/python -m pytest -v`
Expected: tutti PASS — la conformità del dataset buono regge anche con le shape nuove, e ognuno dei 10 casi rotti fa scattare la shape attesa. Se `unreachable-room` o `lock-key-cycle` non scattano, il sospetto n.1 è il property path: verificare che rdflib accetti `(sr:connectedTo|^sr:connectedTo|(^sr:portalInRoom/sr:unlocksRoom))*`; in caso di problemi, spezzare il path in più `UNION` esplicite documentandolo nella nota di studio.

- [ ] **Step 5: Prova "a sensazione" la CLI su un caso rotto**

Run: `.venv/Scripts/python -m tools.validate --data tests/invalid/lock-key-cycle.ttl; echo "exit: $?"`
Expected: report con il messaggio italiano della dipendenza circolare, `exit: 1`.

- [ ] **Step 6: Nota di studio** — `docs/notes/05-shacl-avanzato.md`: differenza tra vincoli core e SPARQL-based (quando servono i secondi); `FILTER NOT EXISTS` con `UNION` come "nessuna fonte esiste"; property path complessi nella shape di raggiungibilità (`^` inverso, `|` alternativa, `*` chiusura riflessivo-transitiva) e il limite dichiarato della `LockKeyCycleShape` (controlla solo il caso diretto: non scopre cicli che passano per più portali — servirebbe la simulazione di M3); perché i property shape anonimi hanno spinto a testare con `resultPath`+`focusNode`; il pattern "micro-mondo rotto per shape".

- [ ] **Step 7: Commit**

```bash
git add shacl/rogue-rules.ttl tests/invalid/ tests/test_validation.py docs/notes/05-shacl-avanzato.md
git commit -m "Task 5: shape di progressione e suite di casi volutamente rotti (M2 completa)"
```

---

### Task 6: Chiusura — documentazione operativa e retrospettiva

**Files:**
- Modify: `CLAUDE.md` (sezione comandi reali), `tasks/todo.md` (spuntare il piano)
- Create: `docs/notes/06-retrospettiva-m1-m2.md`

- [ ] **Step 1: Aggiorna CLAUDE.md**

Nella sezione "REGOLE SPECIFICHE DEL PROGETTO", sostituisci la frase che dice che non c'è codice eseguibile e che la validazione va fatta con tool esterni con:

```markdown
## Comandi

Ambiente: `python -m venv .venv` poi `.venv/Scripts/python -m pip install -r requirements.txt`.

- Validare il mondo: `.venv/Scripts/python -m tools.validate` (exit 0 conforme, 1 violazioni, 2 errore parsing; `--data FILE.ttl` per dataset alternativi)
- Eseguire una query: `.venv/Scripts/python -m tools.query queries/01-rooms-overview.rq` (oppure `--all`)
- Tutti i test: `.venv/Scripts/python -m pytest`
- Un solo test: `.venv/Scripts/python -m pytest tests/test_validation.py::test_authored_dataset_conforms -v`

I casi volutamente rotti stanno in `tests/invalid/` (un micro-mondo per shape); le note di studio in `docs/notes/`.
```

- [ ] **Step 2: Retrospettiva** — `docs/notes/06-retrospettiva-m1-m2.md`: stato raggiunto rispetto a M1/M2 di `obiettivo.md`; cosa hanno rivelato i vincoli eseguibili (prefissi mancanti, sanctum01, portali senza luogo); limiti noti (LockKeyCycleShape solo caso diretto, raggiungibilità che ignora se il portale è apribile); cosa serve per M3 (runtime graph, simulatore).

- [ ] **Step 3: Verifica finale completa**

Run: `.venv/Scripts/python -m pytest -v && .venv/Scripts/python -m tools.validate && .venv/Scripts/python -m tools.query --all`
Expected: tutti i test PASS, `Conforms: True`, sei tabelle popolate.

- [ ] **Step 4: Commit**

```bash
git add CLAUDE.md tasks/todo.md docs/notes/06-retrospettiva-m1-m2.md
git commit -m "Task 6: comandi documentati e retrospettiva M1+M2"
```
